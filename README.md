### Ошибка при компиляции в ассемблер

Инициализация переменной значением несовместимого типа

> [!NOTE]
> Можно проверить с помощью 
> ```
> gcc -E main.c // на предыдущих этапах не было ошибки?
> gcc -S main.c
> ```

