### Ошибка при препроцессинге

Рекурсивный include

> [!IMPORTANT]
> Можно избежать, добавив ```#pragma once``` или ```#ifndef ..., #define ..., endif ...```
>
> Тогда header будет включён только один раз, исключая рекурсию

> [!NOTE]
> Можно проверить с помощью ```gcc -E main.c```

